{
  "name": "hatch-workspace",
  "private": true,
  "version": "0.3.0",
  "license": "GPL-3.0-only",
  "author": "piercezzs",
  "type": "module",
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter hatch desktop:dev",
    "build": "pnpm --filter hatch desktop:build",
    "check": "pnpm -r check",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter hatch test && cargo test --locked --manifest-path apps/hatch/src-tauri/Cargo.toml",
    "desktop:build:local": "node scripts/build-local-macos.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/piercezzs/hatch.git"
  }
}
