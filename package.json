{
  "name": "app-launcher-workspace",
  "private": true,
  "version": "0.2.0",
  "license": "GPL-3.0-only",
  "author": "piercezzs",
  "type": "module",
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter app-launcher desktop:dev",
    "build": "pnpm --filter app-launcher desktop:build",
    "check": "pnpm -r check",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter app-launcher test && cargo test --locked --manifest-path apps/app-launcher/src-tauri/Cargo.toml",
    "desktop:build:local": "node scripts/build-local-macos.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/piercezzs/app-launcher.git"
  }
}
